apiVersion: apps/v1
kind: Deployment
metadata:
  name: course-service-deployment-COLOR_PLACEHOLDER
  labels:
    app: course-service
    color: COLOR_PLACEHOLDER
spec:
  replicas: 2
  selector:
    matchLabels:
      app: course-service
      color: COLOR_PLACEHOLDER
  template:
    metadata:
      labels:
        app: course-service
        color: COLOR_PLACEHOLDER
    spec:
      containers:
      - name: course-service
        image: IMAGE_PLACEHOLDER
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          value: "mysql+pymysql://root:collegepassword@mysql-service:3306/unipiper"
